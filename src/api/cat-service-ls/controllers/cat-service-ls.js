'use strict';

/**
 * cat-service-ls controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cat-service-ls.cat-service-ls');
