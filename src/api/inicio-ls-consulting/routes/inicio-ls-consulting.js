'use strict';

/**
 * inicio-ls-consulting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::inicio-ls-consulting.inicio-ls-consulting');
