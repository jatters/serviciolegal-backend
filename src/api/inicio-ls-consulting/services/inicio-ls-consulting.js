'use strict';

/**
 * inicio-ls-consulting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::inicio-ls-consulting.inicio-ls-consulting');
